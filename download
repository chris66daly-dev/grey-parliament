import Link from "next/link";

export default function Home() {
  return (
    <main style={{ fontFamily: "'Source Serif 4', Georgia, serif" }}>

      {/* NAV */}
      <nav style={{
        background: "#1a1a1a",
        padding: "16px 40px",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        position: "sticky",
        top: 0,
        zIndex: 100
      }}>
        <div style={{ color: "#c9a84c", fontSize: "1.4rem", fontFamily: "'Playfair Display', serif", fontWeight: 900 }}>
          Grey Parliament
        </div>
        <div style={{ display: "flex", gap: "24px", alignItems: "center" }}>
          <Link href="/vote" style={{ color: "#f5f0e8", textDecoration: "none", fontSize: "0.9rem", letterSpacing: "0.08em", textTransform: "uppercase" }}>This Week&apos;s Vote</Link>
          <Link href="/manifesto" style={{ color: "#f5f0e8", textDecoration: "none", fontSize: "0.9rem", letterSpacing: "0.08em", textTransform: "uppercase" }}>Manifesto</Link>
          <Link href="/join" style={{
            background: "#c9a84c",
            color: "#1a1a1a",
            padding: "8px 20px",
            textDecoration: "none",
            fontSize: "0.85rem",
            fontWeight: 700,
            letterSpacing: "0.08em",
            textTransform: "uppercase"
          }}>Join Free</Link>
        </div>
      </nav>

      {/* HERO */}
      <section style={{
        background: "#1a1a1a",
        color: "#f5f0e8",
        padding: "100px 40px",
        textAlign: "center",
        position: "relative",
        overflow: "hidden"
      }}>
        <div style={{
          position: "absolute", top: 0, left: 0, right: 0, bottom: 0,
          background: "repeating-linear-gradient(0deg, transparent, transparent 40px, rgba(201,168,76,0.03) 40px, rgba(201,168,76,0.03) 41px)",
          pointerEvents: "none"
        }} />
        <p style={{ color: "#c9a84c", fontSize: "0.85rem", letterSpacing: "0.4em", textTransform: "uppercase", marginBottom: "20px" }}>
          Est. 2025 — Ash in Makerfield, Lancashire
        </p>
        <h1 style={{
          fontFamily: "'Playfair Display', serif",
          fontSize: "clamp(2.8rem, 7vw, 6rem)",
          fontWeight: 900,
          lineHeight: 1.05,
          marginBottom: "24px"
        }}>
          Your voice is needed<br />
          <span style={{ color: "#c9a84c" }}>now. For your country.</span>
        </h1>
        <p style={{ fontSize: "1.2rem", color: "rgba(245,240,232,0.8)", maxWidth: "640px", margin: "0 auto 16px", fontStyle: "italic" }}>
          Not a petition. Not a protest. Not affiliated to any party. Ever.
        </p>
        <p style={{ fontSize: "1.1rem", color: "rgba(245,240,232,0.9)", maxWidth: "680px", margin: "0 auto 48px", lineHeight: 1.8 }}>
          The over-50s — the biggest voting bloc in Britain — speaking with one organised,
          verified, democratic voice. Every week we vote on the questions politicians avoid.
          Every week the result goes formally to Parliament.
        </p>
        <div style={{ display: "flex", gap: "16px", justifyContent: "center", flexWrap: "wrap" }}>
          <Link href="/join" style={{
            background: "#c9a84c",
            color: "#1a1a1a",
            padding: "16px 40px",
            textDecoration: "none",
            fontSize: "1rem",
            fontWeight: 700,
            letterSpacing: "0.1em",
            textTransform: "uppercase"
          }}>
            Join Free — Have Your Say
          </Link>
          <Link href="/vote" style={{
            border: "1px solid rgba(245,240,232,0.4)",
            color: "#f5f0e8",
            padding: "15px 40px",
            textDecoration: "none",
            fontSize: "1rem",
            letterSpacing: "0.1em",
            textTransform: "uppercase"
          }}>
            This Week&apos;s Vote
          </Link>
        </div>

        <div style={{ marginTop: "80px", display: "flex", justifyContent: "center", gap: "60px", flexWrap: "wrap" }}>
          {[["Age verified", "50+ only"], ["Non-partisan", "No party. Ever."], ["Formally sent", "To Parliament weekly"], ["Your data", "Never sold"]].map(([title, sub]) => (
            <div key={title} style={{ textAlign: "center" }}>
              <div style={{ color: "#c9a84c", fontSize: "0.75rem", letterSpacing: "0.2em", textTransform: "uppercase", marginBottom: "6px" }}>{title}</div>
              <div style={{ color: "rgba(245,240,232,0.6)", fontSize: "0.9rem" }}>{sub}</div>
            </div>
          ))}
        </div>
      </section>

      {/* THIS WEEK'S VOTE */}
      <section style={{ padding: "80px 40px", background: "#f5f0e8" }}>
        <div style={{ maxWidth: "800px", margin: "0 auto", textAlign: "center" }}>
          <p style={{ color: "#c9a84c", fontSize: "0.8rem", letterSpacing: "0.3em", textTransform: "uppercase", marginBottom: "12px" }}>This week&apos;s question</p>
          <h2 style={{ fontFamily: "'Playfair Display', serif", fontSize: "clamp(1.6rem, 3.5vw, 2.8rem)", fontWeight: 700, lineHeight: 1.3, marginBottom: "32px", color: "#1a1a1a" }}>
            Should NHS prescription charges be abolished for all over-60s, regardless of income?
          </h2>
          <div style={{ display: "flex", gap: "16px", justifyContent: "center", marginBottom: "24px", flexWrap: "wrap" }}>
            {["Yes — it should be a right", "No — means testing is fair", "More information needed"].map((opt) => (
              <button key={opt} style={{
                background: "#1a1a1a",
                color: "#f5f0e8",
                border: "none",
                padding: "14px 28px",
                fontSize: "0.95rem",
                cursor: "pointer",
                fontFamily: "'Source Serif 4', serif"
              }}>{opt}</button>
            ))}
          </div>
          <p style={{ color: "#888", fontSize: "0.85rem" }}>You must be a verified member to vote. Results sent to the Health Secretary on Friday.</p>
          <Link href="/join" style={{ display: "inline-block", marginTop: "20px", color: "#c9a84c", fontSize: "0.9rem", fontWeight: 600 }}>
            Join free to cast your vote →
          </Link>
        </div>
      </section>

      {/* THE FIVE RULES */}
      <section style={{ padding: "80px 40px", background: "#2a2a2a", color: "#f5f0e8" }}>
        <div style={{ maxWidth: "900px", margin: "0 auto" }}>
          <p style={{ color: "#c9a84c", fontSize: "0.8rem", letterSpacing: "0.3em", textTransform: "uppercase", marginBottom: "12px", textAlign: "center" }}>Our founding rules</p>
          <h2 style={{ fontFamily: "'Playfair Display', serif", fontSize: "clamp(1.8rem, 3vw, 2.4rem)", fontWeight: 700, textAlign: "center", marginBottom: "48px" }}>
            Five rules. No exceptions. No negotiations.
          </h2>
          <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit, minmax(260px, 1fr))", gap: "24px" }}>
            {[
              ["1", "We affiliate to no political party — ever.", "No donations from any politician, party or campaign. This is constitutionally embedded."],
              ["2", "Every member is age-verified at 50 or over.", "No exceptions. Age verification is what gives our data its credibility."],
              ["3", "One member, one vote.", "No weighting by geography, income or education. Pure democracy."],
              ["4", "All verdicts are published openly.", "Sent formally to the relevant minister every week. Responses published. Non-responses published."],
              ["5", "We answer the questions politicians avoid.", "If it is too awkward for Westminster — that is precisely what we will put to a vote."],
            ].map(([num, title, desc]) => (
              <div key={num} style={{ borderLeft: "3px solid #c9a84c", paddingLeft: "20px" }}>
                <div style={{ color: "#c9a84c", fontFamily: "'Playfair Display', serif", fontSize: "1.8rem", fontWeight: 900, marginBottom: "8px" }}>{num}</div>
                <div style={{ fontWeight: 600, marginBottom: "8px", fontSize: "1rem" }}>{title}</div>
                <div style={{ color: "rgba(245,240,232,0.6)", fontSize: "0.875rem", lineHeight: 1.6 }}>{desc}</div>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* JOIN */}
      <section style={{ padding: "80px 40px", background: "#f5f0e8" }}>
        <div style={{ maxWidth: "700px", margin: "0 auto", textAlign: "center" }}>
          <h2 style={{ fontFamily: "'Playfair Display', serif", fontSize: "clamp(1.8rem, 3.5vw, 2.8rem)", fontWeight: 700, marginBottom: "20px", color: "#1a1a1a" }}>
            The adults are in the room.
          </h2>
          <p style={{ fontSize: "1.1rem", color: "#4a4a4a", marginBottom: "40px", lineHeight: 1.8 }}>
            This is not about left or right. It is about the generation that built this country
            finally having a verified, organised, democratic voice that Parliament cannot ignore.
          </p>
          <div style={{ display: "flex", gap: "20px", justifyContent: "center", flexWrap: "wrap" }}>
            <Link href="/join" style={{
              background: "#1a1a1a",
              color: "#f5f0e8",
              padding: "16px 40px",
              textDecoration: "none",
              fontSize: "1rem",
              fontWeight: 700,
              letterSpacing: "0.08em",
              textTransform: "uppercase"
            }}>Free membership — Join now</Link>
            <Link href="/join?plan=member" style={{
              background: "#c9a84c",
              color: "#1a1a1a",
              padding: "16px 40px",
              textDecoration: "none",
              fontSize: "1rem",
              fontWeight: 700,
              letterSpacing: "0.08em",
              textTransform: "uppercase"
            }}>Official member — £4.99/mo</Link>
          </div>
          <p style={{ marginTop: "20px", color: "#888", fontSize: "0.85rem" }}>
            Free: vote weekly · Official: verdict reports, MP data, priority access
          </p>
        </div>
      </section>

      {/* FOOTER */}
      <footer style={{ background: "#1a1a1a", color: "rgba(245,240,232,0.5)", padding: "40px", textAlign: "center", fontSize: "0.85rem" }}>
        <div style={{ color: "#c9a84c", fontFamily: "'Playfair Display', serif", fontSize: "1.2rem", fontWeight: 700, marginBottom: "12px" }}>Grey Parliament</div>
        <p style={{ marginBottom: "8px" }}>Fiercely independent. Affiliated to no party. Ever.</p>
        <p>Founded in Ash in Makerfield, Lancashire · {new Date().getFullYear()}</p>
        <div style={{ marginTop: "20px", display: "flex", gap: "20px", justifyContent: "center", flexWrap: "wrap" }}>
          <Link href="/manifesto" style={{ color: "rgba(245,240,232,0.5)", textDecoration: "none" }}>Manifesto</Link>
          <Link href="/join" style={{ color: "rgba(245,240,232,0.5)", textDecoration: "none" }}>Join</Link>
          <Link href="/vote" style={{ color: "rgba(245,240,232,0.5)", textDecoration: "none" }}>Vote</Link>
        </div>
      </footer>

    </main>
  );
}
